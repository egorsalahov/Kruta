using System;
using System.Collections.Generic;
using System.Text;

namespace Kruta.Shared.Network.Enums
{
    public enum BuySource
    {
        Baraholka,      // Обычная карта с рынка
        WildMagic,      // Шальная магия (за 3)
        Familiar,       // Свой фамильяр (за 6)
        SlothWand       // Вялая палочка (обычно не покупается, но пусть будет)
    }
}
